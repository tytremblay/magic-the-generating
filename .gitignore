node_modules/
.venv/
.DS_Store
*.log

# local secrets (Gemini API key for art generation)
.env

# local agent config (skills are committed; local settings are not)
.claude/settings.local.json

# build output
public/cards.json
public/decks.json

# MPC print export (large, regenerable via `pnpm export`)
public/print/
